# 入力方法
str = input()
print = (str)
# 整数の場合
sum=int(input())
print = (sum)

# for文の例
# endは必要なしインデントで内容を分ける！
# range内は繰り返す回数
for i in range(1):
    sum = int(input())
    print (sum)

# -----------------------------
# メソッド
input().split() 
# 受け取った値をスペースで区切ってわける。

n = map(int(input()))
# 受け取った値をすべて整数に変化する。
